# =============================================================================
# Python / PyPI dependencies for the tooling / scripts layer of this demo app
# All versions are intentionally pinned to vulnerable releases.
# See docs/CVE-REPORT.md for full details and fix versions.
# =============================================================================

# ---------------------------------------------------------------------------
# CVE-2023-32681 — HIGH  (requests 2.x SSRF via Proxy-Authorization leak)
# Vulnerable: requests < 2.31.0
# Fix:        requests >= 2.31.0
# ---------------------------------------------------------------------------
requests==2.28.2

# ---------------------------------------------------------------------------
# CVE-2022-40897 — HIGH  (setuptools ReDoS)
# Vulnerable: setuptools < 65.5.1
# Fix:        setuptools >= 65.5.1
# ---------------------------------------------------------------------------
setuptools==65.3.0

# ---------------------------------------------------------------------------
# CVE-2023-37276 — HIGH  (aiohttp HTTP request smuggling)
# Vulnerable: aiohttp < 3.8.5
# Fix:        aiohttp >= 3.8.5
# ---------------------------------------------------------------------------
aiohttp==3.8.1

# ---------------------------------------------------------------------------
# CVE-2022-42969 — HIGH  (py ReDoS via SVN revision parsing)
# Vulnerable: py < 1.11.0
# Fix:        py >= 1.11.0
# ---------------------------------------------------------------------------
py==1.10.0

# ---------------------------------------------------------------------------
# CVE-2021-41496 — MEDIUM (NumPy buffer overflow)
# Vulnerable: numpy < 1.21.6
# Fix:        numpy >= 1.21.6
# ---------------------------------------------------------------------------
numpy==1.21.0

# ---------------------------------------------------------------------------
# CVE-2022-34749 — MEDIUM (mistune ReDoS)
# Vulnerable: mistune < 2.0.3
# Fix:        mistune >= 2.0.3
# ---------------------------------------------------------------------------
mistune==3.2.0

# ---------------------------------------------------------------------------
# CVE-2022-21699 — HIGH  (IPython arbitrary code execution)
# Vulnerable: ipython < 7.31.1
# Fix:        ipython >= 7.31.1  (or >= 8.0.1 on Python 3.8+)
# ---------------------------------------------------------------------------
ipython==7.29.0

# ---------------------------------------------------------------------------
# CVE-2023-30861 — HIGH  (Flask session cookie exposure)
# Vulnerable: flask < 2.3.2
# Fix:        flask >= 2.3.2
# ---------------------------------------------------------------------------
Flask==2.2.5

# ---------------------------------------------------------------------------
# CVE-2022-29217 — HIGH  (PyJWT algorithm confusion)
# Vulnerable: PyJWT < 2.4.0
# Fix:        PyJWT >= 2.4.0
# ---------------------------------------------------------------------------
PyJWT==2.3.0

# ---------------------------------------------------------------------------
# CVE-2023-43804 — MEDIUM (urllib3 cookie header leak)
# Vulnerable: urllib3 < 1.26.17  or  < 2.0.6
# Fix:        urllib3 >= 1.26.17
# ---------------------------------------------------------------------------
urllib3==1.26.14

# ---------------------------------------------------------------------------
# CVE-2022-23491 — MEDIUM (certifi trusts malicious roots)
# Vulnerable: certifi < 2022.12.07
# Fix:        certifi >= 2022.12.07
# ---------------------------------------------------------------------------
certifi==2022.9.24

# ---------------------------------------------------------------------------
# Utilities (not vulnerable, included for realism)
# ---------------------------------------------------------------------------
click==8.1.3
python-dotenv==1.0.0
pyyaml==6.0
